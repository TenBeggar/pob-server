package com.tenbeggar.pob.repository;

import com.tenbeggar.pob.entity.ChampionPassiveEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChampionPassiveRepository extends JpaRepository<ChampionPassiveEntity, String> {
}
